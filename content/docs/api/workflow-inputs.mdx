---
title: Workflow Inputs
description: Get available inputs for a deployment
openapi: |
  openapi: 3.0.0
  info:
    title: ComfyDeploy API
    version: 1.0.0
  paths:
    /deployments/{deployment_id}/inputs:
      get:
        summary: Get Workflow Inputs
        description: Get the available input parameters for a specific deployment. This endpoint returns the schema of inputs that can be used when queuing runs for this deployment.
        parameters:
          - name: deployment_id
            in: path
            required: true
            schema:
              type: string
            description: The deployment ID
        responses:
          '200':
            description: Successfully retrieved workflow inputs
            content:
              application/json:
                schema:
                  type: object
                  properties:
                    inputs:
                      type: object
                      additionalProperties:
                        type: object
                        properties:
                          type:
                            type: string
                            example: "string"
                          default:
                            type: string
                            example: "default_value"
                          description:
                            type: string
                            example: "Input description"
                      example:
                        input_name:
                          type: "string"
                          default: "default_value"
                          description: "Input description"
---

Get the available input parameters for a specific deployment. This endpoint returns the schema of inputs that can be used when queuing runs for this deployment.

## Endpoint

```
GET /deployments/{deployment_id}/inputs
```

## Parameters

- `deployment_id` (path, required): The deployment ID

## Response

The response includes all the external input nodes configured in the workflow, along with their types, default values, and validation rules.

```json
{
  "inputs": {
    "input_name": {
      "type": "string",
      "default": "default_value",
      "description": "Input description"
    }
  }
}
```
